const express = require("express");
const router = express.Router();

// here is where you will call the auth code that will route to the server for confirmation? shouldnt be too hard check Ben Awad vid again

module.exports = router;
